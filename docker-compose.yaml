version: '3'

services:
  postgres:
    image: postgres:15.4-alpine
    environment:
      POSTGRES_USER: db_user
      POSTGRES_PASSWORD: db_pass
      POSTGRES_DB: r2dbc_test
    ports:
      - "5432:5432"
